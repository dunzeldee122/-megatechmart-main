pipeline {
    agent any

    stages {
        stage('Clone Repo') {
            steps {
                git 'https://github.com/dunzeldee122/-megatechmart-main.git'
            }
        }

        stage('Deploy (Optional)') {
            steps {
                echo 'Static site ready. You can deploy manually or add FTP/hosting steps here.'
                // Example: Upload files to a server via scp or FTP
                // sh 'scp -r * user@yourserver:/var/www/html'
            }
        }
    }
}
